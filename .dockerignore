Dockerfile
.dockerignore
node_modules
npm-debug.log
README.md
.next
.git
.gitignore
claudes-docs

# Runtime credentials belong outside build layers, including archived subprojects.
**/.env
**/.env.*
!**/.env.example

# Phase 4.5g: vendored sub-projects at repo root with broken symlinks /
# their own toolchains — exclude from the Next.js build context.
nwsl
mobile

# Per-run browser databases and authenticated storage must stay private.
03-Tests/e2e/.runtime
03-Tests/e2e/.auth
